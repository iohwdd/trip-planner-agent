## 1. 流式接口与状态契约

- [x] 1.1 设计并实现聊天回合流式返回协议，定义消息增量、步骤更新、阶段结果、完成和错误事件结构
- [x] 1.2 为现有聊天提交链路增加流式入口或流式协商模式，并保持现有详情查询接口兼容可回退
- [x] 1.3 统一流式事件与会话/回合详情接口的阶段状态词汇、错误语义和阶段性结果字段
- [x] 1.4 补充流式中断恢复、澄清态、失败态和降级态的后端测试

## 2. 后端接口审查与技术场景评审

- [x] 2.1 审查现有聊天、会话、方案和任务接口在成功、澄清、失败、权限受限等场景下的返回契约
- [x] 2.2 收紧关键接口的结构化错误信息、状态字段和结果快照表达，减少前端猜测逻辑
- [x] 2.3 评审会话恢复、并发回合冲突、保存方案前提和失败回写等既有场景实现，并修复不一致处
- [x] 2.4 更新后端 README 或接口说明，记录流式协同路径与关键状态语义

## 3. 工作台信息架构重构

- [x] 3.1 精简聊天窗口顶部结构，移除重复标题信息并重新组织顶部操作按钮的展示方式
- [x] 3.2 收敛工作台在初始态、执行中、澄清中和结果完成态下的整体布局与视觉层级
- [x] 3.3 强化“旅游规划 Agent”产品定位文案与阶段提示，使主界面聚焦用户当前最关心的内容
- [x] 3.4 调整移动端与桌面端下的主工作台布局策略，确保上下文面板与主内容区切换自然

## 4. 结果区与执行过程展示优化

- [x] 4.1 全面审查并修复结果区现有样式问题，包括模块间距、卡片边界、状态样式和响应式错位
- [x] 4.2 按“摘要优先、细节展开”重构结果区模块顺序，统一路线摘要、停靠点、交通、预算、替代方案和修订说明的展示层级
- [x] 4.3 优化执行过程展示，让用户清晰看到当前步骤、已完成阶段、异常步骤以及与阶段结果的关系
- [x] 4.4 为空态、加载态、澄清态、降级态和失败态建立统一展示规则并补充测试覆盖

## 5. 前端流式消费与状态管理

- [x] 5.1 在前端状态管理中接入流式聊天事件，支持增量助手消息、阶段状态更新和阶段结果渐进刷新
- [x] 5.2 保留非流式轮询路径作为回退方案，并确保刷新页面后可恢复到一致的会话/回合状态
- [x] 5.3 联动聊天区、执行过程区和结果区，让三者围绕同一回合状态同步更新
- [x] 5.4 为工作台、结果区和执行过程增加组件/页面测试，覆盖流式执行主链路

## 6. 联调与验收

- [x] 6.1 完成前后端联调，验证“发送消息 -> 流式收到进度 -> 渐进展示结果 -> 完成后恢复详情”主链路
- [x] 6.2 验证澄清、降级、失败和流式中断恢复等关键异常路径的用户体验
- [x] 6.3 对主要结果模块进行视觉验收，确保桌面端和移动端都不存在明显样式 bug 或层级问题
- [x] 6.4 更新变更文档与使用说明，为后续执行 `/opsx:apply` 提供明确实现入口

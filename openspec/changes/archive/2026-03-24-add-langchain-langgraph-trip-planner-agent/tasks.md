## 1. Django 后端基础设施

- [x] 1.1 在 `./backend` 初始化 Django 项目结构、应用拆分、依赖管理和基础运行脚本
- [x] 1.2 添加 LangChain、LangGraph、千问 `qwen3.5-plus` 接入库、Django API 支持库和 schema 校验依赖
- [x] 1.3 实现 Django 配置模块，支持读取千问、高德与百度地图所需的密钥、模型名、超时和运行参数

## 2. 外部数据接入层

- [x] 2.1 定义景点、美食、酒店等统一内部数据模型，以及来源、时间和可用性字段
- [x] 2.2 实现高德地图 POI 搜索适配器，并将结果标准化为内部 POI 结构
- [x] 2.3 实现百度地图地点检索适配器，并将结果标准化为餐饮或本地生活候选结构
- [x] 2.4 实现基于公开地图检索的酒店候选适配器，并将结果标准化为住宿候选结构
- [x] 2.5 为外部数据接入层增加超时、重试、限流保护、短时缓存和失败降级能力

## 3. Agent 工作流与 API

- [x] 3.1 定义旅行规划请求、旅行方案输出和 LangGraph 工作流状态的 Python schema
- [x] 3.2 实现千问 `qwen3.5-plus` 模型适配层，并向 LangChain/LangGraph 暴露统一调用接口
- [x] 3.3 实现 `normalize_input`、实时数据检索节点和 `validate_live_data` 节点，构建可消费的规划上下文
- [x] 3.4 实现 `plan_trip`、`verify_plan_consistency`、`assemble_output` 和 `fallback_or_clarify` 节点，并编排完整 LangGraph
- [x] 3.5 暴露 Django HTTP API，使前端能够提交请求、获取执行状态并接收结构化结果或失败响应

## 4. Vue 前端与执行过程 UI

- [x] 4.1 在 `./frotend` 初始化 Vue 3 项目结构、构建脚本和基础页面骨架
- [x] 4.2 实现旅行规划输入表单，覆盖预算、天数、目的地、偏好和必要的约束条件
- [x] 4.3 实现与后端 API 的请求逻辑，并处理提交、轮询或流式更新、成功和失败状态
- [x] 4.4 设计并实现 Agent 执行过程 UI，展示当前阶段、外部数据源调用状态和关键状态消息
- [x] 4.5 实现结果展示页面，按模块渲染概览、逐日行程、预算拆解、景点、美食、住宿、建议、风险提示和来源信息
- [x] 4.6 建立蓝色系 UI 主题与响应式布局，保证桌面端和移动端都具备清晰层次和可操作性

## 5. 验证与联调

- [x] 5.1 为高德与百度地图适配器编写单元测试或模拟桩测试，验证标准化、失败和降级逻辑
- [x] 5.6 为模型限流或上游异常补充稳定回退逻辑，确保本地仍可完成一次规划请求
- [x] 5.2 为 LangGraph 主流程编写集成测试，验证实时数据检索、结构化输出和一致性校验
- [x] 5.3 为 Django API 编写契约测试，验证成功响应、降级响应和失败响应格式
- [x] 5.4 为前端关键界面状态编写组件或端到端测试，覆盖执行中、成功、部分降级和失败场景
- [x] 5.5 添加前后端联调说明、环境变量示例和第三方凭证前置要求，确保本地可以完成一次完整旅行规划流程

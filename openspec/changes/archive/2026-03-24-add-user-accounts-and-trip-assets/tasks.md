## 1. 数据层与项目基础

- [x] 1.1 为 Django 增加 MySQL、JWT、验证码发送所需依赖，并补充 `trip_assistant` 相关环境变量与本地 SQLite 回退配置
- [x] 1.2 启用 Django 认证相关应用与配置，确定用户模型、认证后端和 API 鉴权方式
- [x] 1.3 新增 `User`、`AuthCode`、`ChatSession`、`ChatTurn`、`TripPlan`、`PlanningJob`、`AnalyticsEvent` 等 ORM 模型与迁移脚本
- [x] 1.4 抽象并实现基于 ORM 的 repository/service 层，替换现有内存 `ChatSessionStore` 与 `PlannerRunStore`

## 2. 账号体系与游客迁移

- [x] 2.1 实现游客标识生成、解析与请求归属识别，使未登录用户也能创建和恢复临时会话
- [x] 2.2 实现邮箱验证码发送、验证码校验登录、当前登录态查询和退出登录接口
- [x] 2.3 为验证码发送和登录校验增加有效期、频控、限流和统一错误返回
- [x] 2.4 实现登录成功后的游客会话与方案迁移逻辑，并保证迁移后资产归属正确
- [x] 2.5 为认证、游客迁移和鉴权隔离补充后端测试

## 3. 会话与方案资产化

- [x] 3.1 将聊天消息、回合步骤、已确认约束、最新结果和错误信息持久化到 `ChatSession` / `ChatTurn`
- [x] 3.2 实现会话列表、详情、最近会话恢复、重命名、删除和继续编辑接口
- [x] 3.3 让聊天输入与快捷表单提交统一写入当前活动会话，并复用同一规划上下文
- [x] 3.4 实现方案保存为草案或最终方案、版本号递增、方案列表、方案详情、删除和从历史方案继续修订接口
- [x] 3.5 为会话恢复、版本化方案和用户数据隔离补充后端测试

## 4. 可靠任务执行与观测

- [x] 4.1 将 `/api/plans/` 与聊天回合提交改为创建 `PlanningJob` 持久化任务，而不是启动进程内线程
- [x] 4.2 实现数据库队列 worker，支持领取任务、执行 LangGraph 工作流、写回步骤、结果和失败信息
- [x] 4.3 实现任务超时、僵尸任务回收、失败状态落库与前端可见的状态映射
- [x] 4.4 记录会话数、回合数、方案保存数、澄清轮次、游客转登录事件，以及模型和数据源耗时指标
- [x] 4.5 为队列执行、服务重启恢复、失败/超时场景补充后端集成测试

## 5. 前端工作台产品化

- [x] 5.1 引入 Vue Router 和应用壳层，补齐产品标题“旅游规划 Agent”、登录入口、用户菜单和游客提示
- [x] 5.2 实现前端登录态管理、JWT 持久化、游客 token 持久化和刷新后的状态恢复
- [x] 5.3 实现“我的会话”列表、最近会话自动打开和从历史会话继续编辑的工作台联动
- [x] 5.4 实现保存方案、草案/最终状态展示、“我的方案”列表、方案详情和从方案继续修订入口
- [x] 5.5 保持快捷表单作为聊天工作台次级入口，并确保其提交继续进入当前会话
- [x] 5.6 调整响应式 UI、空状态、错误状态和执行状态展示，使游客态与登录态路径清晰可用

## 6. 验证、联调与文档

- [x] 6.1 更新 README、环境变量示例和启动说明，补充 MySQL、worker、验证码通道和登录流程配置
- [x] 6.2 增加前端测试，覆盖登录态恢复、会话列表、方案保存、继续修订和权限受限提示
- [x] 6.3 增加端到端联调用例，验证“游客规划 -> 登录迁移 -> 保存草案 -> 打开历史会话继续编辑”主链路
- [x] 6.4 在远程 MySQL `trip_assistant` 环境完成迁移演练与基础数据隔离验证
